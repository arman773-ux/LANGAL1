# 🎙️ বাংলা টেক্সট-টু-স্পিচ (TTS) সিস্টেম

## সংক্ষিপ্ত বিবরণ

এটি একটি উচ্চমানের বাংলা TTS (Text-to-Speech) সিস্টেম যা **Hugging Face AI মডেল** এবং **ব্রাউজার TTS** উভয় সাপোর্ট করে। ক্লিক করলে বাংলা টেক্সট পড়ে শোনায়।

## ✨ বৈশিষ্ট্য

- ✅ **উচ্চমানের AI TTS**: Meta এর MMS (Massively Multilingual Speech) মডেল
- ✅ **অফলাইন সাপোর্ট**: ব্রাউজার TTS ব্যবহার করে
- ✅ **স্বয়ংক্রিয় Fallback**: AI TTS ব্যর্থ হলে ব্রাউজার TTS ব্যবহার হবে
- ✅ **রিয়েল-টাইম প্রোগ্রেস**: লোডিং এবং প্লেয়িং স্ট্যাটাস দেখায়
- ✅ **সহজ ব্যবহার**: শুধু একটি ক্লিক
- ✅ **কাস্টমাইজেবল**: Rate, pitch, volume নিয়ন্ত্রণ করা যায়

## 🚀 দ্রুত শুরু

### 1. Component Import করুন

```tsx
import { AdvancedTTSButton } from "@/components/ui/advanced-tts-button";
```

### 2. ব্যবহার করুন

```tsx
<AdvancedTTSButton
  text="আপনার বাংলা টেক্সট এখানে"
  showLabel
  useHuggingFace={true}
/>
```

## 📋 AI TTS সেটআপ (ঐচ্ছিক)

AI TTS ব্যবহারের জন্য Hugging Face API কনফিগার করতে হবে:

### ধাপ ১: Hugging Face একাউন্ট

1. [huggingface.co](https://huggingface.co) এ যান
2. সাইন আপ করুন (বিনামূল্যে)

### ধাপ ২: API Token তৈরি

1. Settings → [Access Tokens](https://huggingface.co/settings/tokens) এ যান
2. "New token" ক্লিক করুন
3. নাম দিন এবং "read" পারমিশন সিলেক্ট করুন
4. টোকেন কপি করুন

### ধাপ ৩: Environment Variable সেট করুন

প্রজেক্টের রুট ফোল্ডারে `.env` ফাইল তৈরি করুন:

```bash
VITE_HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxx
```

**নোট**: `.env.example` ফাইল দেখুন উদাহরণের জন্য।

### ধাপ ৪: Dev Server রিস্টার্ট করুন

```bash
npm run dev
```

## 🎯 ব্যবহারের উদাহরণ

### বেসিক ব্যবহার

```tsx
// সবচেয়ে সহজ ব্যবহার
<AdvancedTTSButton text="আসসালামু আলাইকুম" />
```

### লেবেল সহ

```tsx
<AdvancedTTSButton
  text="ধান চাষের পরামর্শ..."
  showLabel
  size="default"
  variant="default"
/>
```

### লেখকের নাম সহ

```tsx
<AdvancedTTSButton
  text="আজকের পরামর্শ..."
  authorName="কৃষি বিশেষজ্ঞ"
  showLabel
/>
```

### শুধুমাত্র ব্রাউজার TTS

```tsx
<AdvancedTTSButton text="অফলাইন TTS" useHuggingFace={false} showLabel />
```

### আইকন বাটন

```tsx
<AdvancedTTSButton text="টেক্সট..." size="icon" variant="ghost" />
```

## 🔧 API রেফারেন্স

### AdvancedTTSButton Props

| Props            | Type                                  | Default     | বর্ণনা                   |
| ---------------- | ------------------------------------- | ----------- | ------------------------ |
| `text`           | `string`                              | (required)  | পড়ার জন্য টেক্সট        |
| `authorName`     | `string?`                             | -           | লেখকের নাম (ঐচ্ছিক)      |
| `showLabel`      | `boolean?`                            | `false`     | লেবেল দেখাবে কিনা        |
| `useHuggingFace` | `boolean?`                            | `true`      | AI TTS ব্যবহার করবে কিনা |
| `size`           | `'default' \| 'sm' \| 'lg' \| 'icon'` | `'icon'`    | বাটনের সাইজ              |
| `variant`        | ButtonVariant                         | `'outline'` | বাটনের ভ্যারিয়েন্ট      |
| `className`      | `string?`                             | -           | কাস্টম CSS ক্লাস         |

### TTS Service Methods

```typescript
import { banglaTTSService } from "@/services/ttsService";

// টেক্সট পড়ুন
await banglaTTSService.speak("টেক্সট", {
  rate: 0.85,
  pitch: 1.0,
  volume: 1.0,
  useHuggingFace: true,
});

// বন্ধ করুন
banglaTTSService.stop();

// চেক করুন প্লে হচ্ছে কিনা
const playing = banglaTTSService.isPlaying();

// স্ট্যাটাস ট্র্যাক করুন
const unsubscribe = banglaTTSService.onStatusChange((status) => {
  console.log(status.isPlaying, status.isLoading, status.progress);
});
```

## 🤖 সাপোর্টেড AI মডেল

1. **facebook/mms-tts-ben** (Primary)

   - Meta এর Massively Multilingual Speech
   - সবচেয়ে ভালো মান
   - Natural বাংলা উচ্চারণ

2. **mnatrb/VitsModel-Bangla-Female** (Secondary)

   - বাংলা নারী কণ্ঠস্বর
   - VITS আর্কিটেকচার

3. **Browser Web Speech API** (Fallback)
   - অফলাইন কাজ করে
   - কোন API প্রয়োজন নেই

## 📱 ব্রাউজার সাপোর্ট

| Browser | AI TTS | Browser TTS |
| ------- | ------ | ----------- |
| Chrome  | ✅     | ✅          |
| Edge    | ✅     | ✅          |
| Firefox | ✅     | ⚠️ Limited  |
| Safari  | ✅     | ✅          |
| Opera   | ✅     | ✅          |

## 🎨 UI States

Component বিভিন্ন স্টেট দেখায়:

- **Idle**: 🔊 শুনুন (বা ✨ AI দিয়ে শুনুন)
- **Loading**: ⌛ প্রস্তুত হচ্ছে... (Progress bar সহ)
- **Playing**: 🔇 বন্ধ করুন

## 💡 ব্যবহারের ক্ষেত্র

1. **কৃষি পরামর্শ**: পোস্ট পড়ে শোনানো
2. **নিউজ ফিড**: খবর শোনা
3. **মার্কেটপ্লেস**: পণ্যের বিবরণ শোনা
4. **নির্দেশনা**: ব্যবহারের নির্দেশনা শোনা
5. **নিরক্ষর ব্যবহারকারী**: যারা পড়তে পারেন না

## 🔒 নিরাপত্তা

- API Key সবসময় environment variable এ রাখুন
- `.env` ফাইল কখনো git এ commit করবেন না
- `.gitignore` এ `.env` যুক্ত আছে কিনা নিশ্চিত করুন

## 🐛 Troubleshooting

### AI TTS কাজ করছে না

1. `.env` ফাইলে API key আছে কিনা চেক করুন
2. Dev server রিস্টার্ট করুন
3. Browser console এ error চেক করুন
4. HuggingFace API status চেক করুন: [status.huggingface.co](https://status.huggingface.co)

### Browser TTS বাংলায় পড়ছে না

1. Chrome/Edge ব্যবহার করুন (সবচেয়ে ভালো সাপোর্ট)
2. System language settings এ বাংলা যোগ করুন
3. Chrome voice settings চেক করুন

### অডিও প্লে হচ্ছে না

1. Browser autoplay policy চেক করুন
2. User interaction এর পরে TTS trigger করুন
3. Device volume চেক করুন

## 📊 Performance

### AI TTS

- প্রথম লোড: ~2-5 সেকেন্ড (মডেল ডাউনলোড)
- পরবর্তী কল: ~500ms-1s
- ক্যাশিং: Hugging Face API লেভেলে

### Browser TTS

- তাৎক্ষণিক (~50-100ms)
- কোন নেটওয়ার্ক কল নেই
- অফলাইন কাজ করে

## 🔄 Migration Guide

### পুরাতন TTSButton থেকে Migrate করা

**Before:**

```tsx
import { TTSButton } from "@/components/ui/tts-button";

<TTSButton text="টেক্সট" />;
```

**After:**

```tsx
import { AdvancedTTSButton } from "@/components/ui/advanced-tts-button";

<AdvancedTTSButton
  text="টেক্সট"
  useHuggingFace={true} // AI TTS enable
  showLabel // Optional: লেবেল দেখাতে চাইলে
/>;
```

## 🎓 Demo Page

সম্পূর্ণ demo দেখতে:

```
http://localhost:5173/tts-demo
```

অথবা প্রজেক্টে navigate করুন: `/tts-demo`

## 📚 আরও তথ্য

- [Hugging Face Documentation](https://huggingface.co/docs)
- [Meta MMS Model](https://huggingface.co/facebook/mms-tts-ben)
- [Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)

## 📝 License

MIT License - মুক্তভাবে ব্যবহার করুন!

## 🤝 Contributing

Issues এবং Pull Requests স্বাগতম!

---

**তৈরি করেছেন ❤️ দিয়ে বাংলা কৃষি প্রযুক্তির জন্য**
