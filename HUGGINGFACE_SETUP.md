# 🔧 Hugging Face API সেটআপ গাইড

## কেন Hugging Face?

Hugging Face AI TTS ব্যবহার করলে:

- ✅ **প্রাকৃতিক উচ্চারণ**: Meta's MMS মডেল - সেরা বাংলা TTS
- ✅ **সব ব্রাউজারে কাজ করবে**: Voice pack ইনস্টল লাগবে না
- ✅ **উচ্চমান**: নিখুঁত বাংলা উচ্চারণ

## ⚡ দ্রুত সেটআপ (মাত্র ২ মিনিট!)

### Step 1: Hugging Face একাউন্ট তৈরি

1. যান: https://huggingface.co/join
2. Email দিয়ে সাইন আপ করুন (একদম ফ্রি!)
3. Email verify করুন

### Step 2: API Token নিন

1. লগইন করার পর যান: https://huggingface.co/settings/tokens
2. "New token" button এ ক্লিক করুন
3. Token name দিন: `langal-tts` (যেকোনো নাম)
4. Type: **Read** select করুন
5. "Generate token" ক্লিক করুন
6. Token **কপি করে রাখুন** (এটি আর দেখতে পাবেন না!)

### Step 3: Project এ Configure করুন

#### Option A: `.env` ফাইল তৈরি করুন

```bash
# Project root এ .env ফাইল তৈরি করুন
cd "d:\Software Engineering\3.1\SE 3112 - Software Project Lab II\langal demo\Updated\langol-krishi-sahayak"

# .env ফাইলে এটি যোগ করুন:
VITE_HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

#### Option B: Manual তে ফাইল তৈরি

1. Project root folder এ নতুন file তৈরি করুন: `.env`
2. এই লাইন যোগ করুন:

```env
VITE_HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

3. `hf_xxxxx` এর জায়গায় আপনার actual token বসান
4. Save করুন

### Step 4: Dev Server রিস্টার্ট

```bash
# Terminal বন্ধ করুন (Ctrl+C)
# আবার চালু করুন:
npm run dev
```

### Step 5: টেস্ট করুন! 🎉

1. Browser এ যান: `http://localhost:5173/tts-demo`
2. "AI TTS (Hugging Face)" button দেখবেন
3. ক্লিক করে টেস্ট করুন
4. শুনুন সবচেয়ে ভালো বাংলা উচ্চারণ! 🎙️

---

## ✅ সফলভাবে সেটআপ হয়েছে কিনা চেক করুন

### Hugging Face কনফিগার করা আছে:

- ✅ Green card দেখাবে: "Hugging Face AI সক্রিয়"
- ✅ AI TTS button এ sparkle icon (✨) থাকবে
- ✅ "AI দিয়ে শুনুন" text দেখাবে

### Hugging Face কনফিগার করা নেই:

- ⚠️ Yellow/amber card দেখাবে
- ⚠️ Toast notification: "Hugging Face API কনফিগার করা নেই"
- 🔄 Automatically browser TTS ব্যবহার হবে

---

## 🔒 Security Note

- ✅ `.env` ফাইল **git ignored** - commit হবে না
- ✅ Token **শুধু আপনার local machine** এ থাকবে
- ✅ কোন billing/payment লাগবে না - সম্পূর্ণ ফ্রি!

---

## 🐛 Troubleshooting

### "Hugging Face API কনফিগার করা নেই" দেখাচ্ছে?

**Check 1: `.env` ফাইল সঠিক জায়গায় আছে?**

```
langol-krishi-sahayak/
├── .env          ← এখানে থাকতে হবে!
├── package.json
├── src/
└── ...
```

**Check 2: Variable name সঠিক?**

```env
VITE_HUGGINGFACE_API_KEY=hf_xxxxx
# ⚠️ "VITE_" দিয়ে শুরু করতে হবে!
```

**Check 3: Dev server রিস্টার্ট করেছেন?**

```bash
# Ctrl+C চেপে বন্ধ করুন
# আবার চালু করুন:
npm run dev
```

**Check 4: Token সঠিক?**

- Token `hf_` দিয়ে শুরু হয়
- Space বা extra character নেই
- Copy করার সময় পুরোটা কপি হয়েছে

### Console এ check করুন:

```javascript
// Browser Console এ type করুন:
import.meta.env.VITE_HUGGINGFACE_API_KEY;
// Output: "hf_xxxxx" দেখাবে (token configured)
// Output: undefined দেখালে problem!
```

---

## 📊 API Usage Limits

Hugging Face Free tier:

- ✅ **30,000 requests/month** - একদম যথেষ্ট!
- ✅ Rate limit: ~10 requests/minute
- ✅ কোন credit card লাগবে না

---

## 🎯 Next Steps

Token সেটআপ হয়ে গেলে:

1. `/tts-demo` page এ যান
2. AI TTS button ব্যবহার করুন
3. উচ্চমানের বাংলা TTS উপভোগ করুন! 🎉

---

**সাহায্য লাগলে:**

- দেখুন: [BANGLA_TTS_GUIDE.md](./BANGLA_TTS_GUIDE.md)
- Hugging Face docs: https://huggingface.co/docs
